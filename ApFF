╔══════════════════════════════════════════╗
║         Visual OPTS  (Mobile)            ║
║  Tap "Spam Closest" to spam all cmds     ║
║  Drag any panel to move it               ║
╚══════════════════════════════════════════╝
local Players           = game:GetService("Players")
local CoreGui           = game:GetService("CoreGui")
local UserInputService  = game:GetService("UserInputService")
local TweenService      = game:GetService("TweenService")
local RunService        = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService       = game:GetService("HttpService")
local lp = Players.LocalPlayer
local core = { remote = nil }
task.spawn(function()
    if not lp.Character then lp.CharacterAdded:Wait() end
    task.wait(1)
    local net = ReplicatedStorage:WaitForChild("Packages"):WaitForChild("Net")
    local ch  = net:GetChildren()
    local idx, n2i = {}, {}
    for i,o in ipairs(ch) do idx[i]=o; n2i[o.Name]=i end
    local a = n2i["RF/a0e78691-cb9b-4efc-ac08-9c06fea70059"]
    if a then local r=idx[a+1]; if r then core.remote=r end end
end)
local CFG = "FrySpammerMobile.json"
local pos = { ax=8,ay=8, lx=8,ly=50, rx=105,ry=50 }
do
    if readfile then
        local ok,raw = pcall(readfile,CFG)
        if ok and raw and raw~="" then
            local ok2,t = pcall(HttpService.JSONDecode,HttpService,raw)
            if ok2 and type(t)=="table" then for k,v in pairs(t) do pos[k]=v end end
        end
    end
end
local function savePos() if writefile then pcall(writefile,CFG,HttpService:JSONEncode(pos)) end end
local CMDS = {"rocket","ragdoll","balloon","inverse","nightvision","jail","control","tiny","jumpscare","morph"}
local DUR  = {rocket=119,ragdoll=29,balloon=29,inverse=59,nightvision=59,jail=59,control=59,tiny=59,jumpscare=59,morph=59}
local cooldowns = {}
local cdLabels  = {}
local selPlayer = nil
local cardMap   = {}
if CoreGui:FindFirstChild("FryGui") then CoreGui.FryGui:Destroy() end
local SG = Instance.new("ScreenGui")
SG.Name="FryGui"; SG.ResetOnSpawn=false
SG.ZIndexBehavior=Enum.ZIndexBehavior.Global
SG.IgnoreGuiInset=true; SG.Parent=CoreGui

-- ══════════════════════════════════════════
-- NUEVA PALETA DE COLORES - ECLIPSE NARANJA
-- ══════════════════════════════════════════
local C = {
    panel  = Color3.fromRGB(18, 16, 14),
    header = Color3.fromRGB(14, 12, 10),
    card   = Color3.fromRGB(24, 20, 16),
    border = Color3.fromRGB(180, 100, 40),
    selBdr = Color3.fromRGB(255, 150, 50),
    minBtn = Color3.fromRGB(30, 26, 22),
    iBtn   = Color3.fromRGB(35, 28, 20),
    spam   = Color3.fromRGB(200, 100, 30),
    spam2  = Color3.fromRGB(255, 140, 50),
    green  = Color3.fromRGB(50, 220, 80),
    red    = Color3.fromRGB(230, 60, 50),
    white  = Color3.new(1,1,1),
    grey   = Color3.fromRGB(170,155,140),
    glow   = Color3.fromRGB(255, 130, 40),
    accent = Color3.fromRGB(220, 120, 45),
}

local HDR  = 22
local AW   = 200
local AH   = 50
local LW   = 96
local LH   = 175
local RW   = 96
local ROW  = 13
local RH   = HDR + 4 + #CMDS*(ROW+2) + 4
local tH = 7
local tB = 6
local tS = 5
local tC = 6
local tSt= 6
local tSp= 7

-- ══════════════════════════════════════════
-- FONDO GENERAL
-- ══════════════════════════════════════════
local bgFrame = Instance.new("ImageLabel")
bgFrame.Name = "BackgroundImage"
bgFrame.Size = UDim2.new(1, 0, 1, 0)
bgFrame.Position = UDim2.new(0, 0, 0, 0)
bgFrame.BackgroundTransparency = 1
bgFrame.Image = "rbxassetid://112002576136361"
bgFrame.ScaleType = Enum.ScaleType.Crop
bgFrame.ZIndex = 0
bgFrame.Parent = SG

local bgOverlay = Instance.new("Frame")
bgOverlay.Name = "BackgroundOverlay"
bgOverlay.Size = UDim2.new(1, 0, 1, 0)
bgOverlay.Position = UDim2.new(0, 0, 0, 0)
bgOverlay.BackgroundColor3 = Color3.fromRGB(10, 8, 6)
bgOverlay.BackgroundTransparency = 0.35
bgOverlay.ZIndex = 0
bgOverlay.Parent = SG

local function corner(p,r)
    local c=Instance.new("UICorner"); c.CornerRadius=UDim.new(0,r or 6); c.Parent=p
end
local function stroke(p,t,col)
    local s=Instance.new("UIStroke")
    s.Thickness=t or 1; s.ApplyStrokeMode=Enum.ApplyStrokeMode.Border
    s.Color=col or C.border; s.Parent=p; return s
end
local function glowStroke(p, col)
    local s=Instance.new("UIStroke")
    s.Thickness=2; s.ApplyStrokeMode=Enum.ApplyStrokeMode.Border
    s.Color=col or C.glow; s.Transparency=0.5; s.Parent=p; return s
end
local function shadow(p)
    local img=Instance.new("ImageLabel")
    img.Name="Shadow"; img.BackgroundTransparency=1
    img.Image="rbxassetid://1316045217"
    img.ImageColor3=Color3.new(0,0,0); img.ImageTransparency=0.6
    img.ScaleType=Enum.ScaleType.Slice; img.SliceCenter=Rect.new(10,10,118,118)
    img.Size=UDim2.new(1,10,1,10); img.Position=UDim2.new(0,-5,0,-5)
    img.ZIndex=p.ZIndex-1; img.Parent=p
end
local function lbl(parent,props)
    local l=Instance.new("TextLabel"); l.BackgroundTransparency=1
    for k,v in pairs(props) do l[k]=v end; l.Parent=parent; return l
end
local function draggable(handle, frame, xk, yk)
    local on,ds,sp=false,nil,nil
    handle.InputBegan:Connect(function(i)
        if i.UserInputType==Enum.UserInputType.MouseButton1
        or i.UserInputType==Enum.UserInputType.Touch then
            on=true; ds=i.Position; sp=frame.Position
        end
    end)
    UserInputService.InputChanged:Connect(function(i)
        if not on then return end
        if i.UserInputType==Enum.UserInputType.MouseMovement
        or i.UserInputType==Enum.UserInputType.Touch then
            local d=i.Position-ds
            local cam=workspace.CurrentCamera.ViewportSize
            local gs=frame.AbsoluteSize
            frame.Position=UDim2.new(0,math.clamp(sp.X.Offset+d.X,0,cam.X-gs.X),
                                       0,math.clamp(sp.Y.Offset+d.Y,0,cam.Y-gs.Y))
        end
    end)
    UserInputService.InputEnded:Connect(function(i)
        if on and(i.UserInputType==Enum.UserInputType.MouseButton1
               or i.UserInputType==Enum.UserInputType.Touch) then
            on=false
            pos[xk]=frame.Position.X.Offset; pos[yk]=frame.Position.Y.Offset; savePos()
        end
    end)
end

-- ══════════════════════════════════════════
-- ANIMACIÓN DE ENTRADA
-- ══════════════════════════════════════════
local function animateEntry(frame, delay)
    frame.BackgroundTransparency = 1
    local origPos = frame.Position
    frame.Position = UDim2.new(origPos.X.Scale, origPos.X.Offset, origPos.Y.Scale, origPos.Y.Offset + 20)
    task.delay(delay or 0, function()
        TweenService:Create(frame, TweenInfo.new(0.4, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
            BackgroundTransparency = 0,
            Position = origPos
        }):Play()
    end)
end

local function mkPanel(x,y,w,h)
    local f=Instance.new("Frame")
    f.Size=UDim2.new(0,w,0,h); f.Position=UDim2.new(0,x,0,y)
    f.BackgroundColor3=C.panel; f.BorderSizePixel=0
    f.ClipsDescendants=true; f.Active=true; f.Parent=SG
    corner(f,8); stroke(f,1.5,C.border); glowStroke(f,C.glow); shadow(f)
    animateEntry(f, 0.1)
    return f
end
local function mkHeader(panel, title)
    local h=Instance.new("Frame")
    h.Size=UDim2.new(1,0,0,HDR); h.BackgroundColor3=C.header
    h.BorderSizePixel=0; h.ZIndex=3; h.Parent=panel
    corner(h,8)
    local fill=Instance.new("Frame")
    fill.Size=UDim2.new(1,0,0,6); fill.Position=UDim2.new(0,0,1,-6)
    fill.BackgroundColor3=C.header; fill.BorderSizePixel=0; fill.ZIndex=3; fill.Parent=h
    lbl(h,{Position=UDim2.new(0,5,0,0),Size=UDim2.new(1,-22,1,0),
           Font=Enum.Font.GothamBlack, TextSize=tH, TextColor3=C.accent,
           TextXAlignment=Enum.TextXAlignment.Left, Text=title, ZIndex=4})
    local btn=Instance.new("TextButton")
    btn.Size=UDim2.new(0,13,0,13); btn.Position=UDim2.new(1,-16,0.5,-6)
    btn.BackgroundColor3=C.minBtn; btn.Font=Enum.Font.GothamBlack
    btn.Text="-"; btn.TextColor3=C.accent; btn.TextSize=9
    btn.ZIndex=5; btn.Parent=h
    corner(btn,3); stroke(btn,1,C.border)
    btn.MouseEnter:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3=C.glow}):Play()
    end)
    btn.MouseLeave:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3=C.minBtn}):Play()
    end)
    return h, btn
end
local function mkMinimize(f, btn)
    local min=false; local orig=f.Size; local mini=UDim2.new(0,orig.X.Offset,0,HDR)
    btn.MouseButton1Click:Connect(function()
        min=not min
        TweenService:Create(f,TweenInfo.new(0.2,Enum.EasingStyle.Quint),
            {Size=min and mini or orig}):Play()
        btn.Text=min and "+" or "-"
    end)
end

-- ══════════════════════════════════════════
-- ADMIN PANEL
-- ══════════════════════════════════════════
local AP = mkPanel(pos.ax, pos.ay, AW, AH)
local apHdr, apMinBtn = mkHeader(AP,"Visual OPTS")
mkMinimize(AP, apMinBtn)
draggable(apHdr, AP,"ax","ay")

-- Logo junto al título
local logoImg = Instance.new("ImageLabel")
logoImg.Name = "Logo"
logoImg.Size = UDim2.new(0, 16, 0, 16)
logoImg.Position = UDim2.new(0, 6, 0.5, -8)
logoImg.BackgroundTransparency = 1
logoImg.Image = "rbxassetid://104937666038724"
logoImg.ScaleType = Enum.ScaleType.Fit
logoImg.ZIndex = 5
logoImg.Parent = apHdr

local apRow=Instance.new("Frame")
apRow.Size=UDim2.new(1,-6,0,AH-HDR-4)
apRow.Position=UDim2.new(0,3,0,HDR+2)
apRow.BackgroundColor3=C.card; apRow.BorderSizePixel=0; apRow.Parent=AP
corner(apRow,6); stroke(apRow,1,C.border)

local apAva=Instance.new("ImageLabel")
apAva.Size=UDim2.new(0,18,0,18); apAva.Position=UDim2.new(0,3,0.5,-9)
apAva.BackgroundColor3=C.card; apAva.Image=""; apAva.Parent=apRow
corner(apAva,9)

local apName=lbl(apRow,{Position=UDim2.new(0,24,0,2),Size=UDim2.new(0,75,0,10),
    Font=Enum.Font.GothamBold,TextSize=tB,TextColor3=C.white,
    TextXAlignment=Enum.TextXAlignment.Left,Text=lp.DisplayName,ZIndex=2})
local apUser=lbl(apRow,{Position=UDim2.new(0,24,0,12),Size=UDim2.new(0,75,0,8),
    Font=Enum.Font.Gotham,TextSize=tS,TextColor3=C.grey,
    TextXAlignment=Enum.TextXAlignment.Left,Text="(@"..lp.Name..")",ZIndex=2})

local ACTS={{icon="👉",cmd="control"},{icon="🔒",cmd="jail"},
            {icon="🚀",cmd="rocket"}, {icon="✂️",cmd="ragdoll"},
            {icon="🎈",cmd="balloon"}}
local ib=14; local ig=2
local bxStart = (AW-6) - #ACTS*(ib+ig) + ig
for i,act in ipairs(ACTS) do
    local b=Instance.new("TextButton")
    b.Size=UDim2.new(0,ib,0,ib)
    b.Position=UDim2.new(0,bxStart+(i-1)*(ib+ig),0.5,-ib//2)
    b.BackgroundColor3=C.iBtn; b.Font=Enum.Font.GothamBlack
    b.Text=act.icon; b.TextSize=9; b.ZIndex=4; b.Parent=apRow
    corner(b,4); stroke(b,1,C.border)
    b.MouseEnter:Connect(function()
        TweenService:Create(b, TweenInfo.new(0.15), {BackgroundColor3=C.glow}):Play()
    end)
    b.MouseLeave:Connect(function()
        TweenService:Create(b, TweenInfo.new(0.15), {BackgroundColor3=C.iBtn}):Play()
    end)
    b.MouseButton1Click:Connect(function()
        if not core.remote then return end
        local target=selPlayer
        if not target then
            local hrp=lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                local best,bd=nil,math.huge
                for _,p in ipairs(Players:GetPlayers()) do
                    if p~=lp and p.Character then
                        local h=p.Character:FindFirstChild("HumanoidRootPart")
                        if h then local d=(h.Position-hrp.Position).Magnitude
                            if d<bd then bd=d; best=p end end
                    end
                end
                target=best
            end
        end
        if target then
            local now=tick()
            pcall(function()
                core.remote:InvokeServer("f888ee6e-c86d-46e1-93d7-0639d6635d42",target,act.cmd)
            end)
            cooldowns[act.cmd]=now+(DUR[act.cmd] or 30)
            TweenService:Create(b,TweenInfo.new(0.07),{BackgroundColor3=C.accent}):Play()
            task.delay(0.2,function() TweenService:Create(b,TweenInfo.new(0.15),{BackgroundColor3=C.iBtn}):Play() end)
        end
    end)
end
task.spawn(function()
    while apRow.Parent do
        task.wait(0.5)
        local t=selPlayer or lp
        apName.Text=t.DisplayName; apUser.Text="(@"..t.Name..")"
        local ok,img=pcall(Players.GetUserThumbnailAsync,Players,t.UserId,
            Enum.ThumbnailType.HeadShot,Enum.ThumbnailSize.Size48x48)
        if ok then apAva.Image=img end
    end
end)

-- ══════════════════════════════════════════
-- LEFT PANEL (PLAYER LIST)
-- ══════════════════════════════════════════
local LP = mkPanel(pos.lx, pos.ly, LW, LH)
local lpHdr, lpMinBtn = mkHeader(LP,"Visual OPTS")
mkMinimize(LP, lpMinBtn)
draggable(lpHdr, LP,"lx","ly")

local spamBtnH = 17
local scroll=Instance.new("ScrollingFrame")
scroll.Position=UDim2.new(0,3,0,HDR+3)
scroll.Size=UDim2.new(1,-6,1,-HDR-spamBtnH-9)
scroll.BackgroundColor3=Color3.fromRGB(12, 10, 8)
scroll.BorderSizePixel=0
scroll.ScrollBarThickness=2
scroll.ScrollBarImageColor3=C.accent
scroll.AutomaticCanvasSize=Enum.AutomaticSize.Y
scroll.CanvasSize=UDim2.new(0,0,0,0)
scroll.Parent=LP
corner(scroll,6); stroke(scroll,1,C.border)

local ull=Instance.new("UIListLayout")
ull.Padding=UDim.new(0,2); ull.SortOrder=Enum.SortOrder.Name
ull.HorizontalAlignment=Enum.HorizontalAlignment.Center; ull.Parent=scroll
local upad=Instance.new("UIPadding")
upad.PaddingTop=UDim.new(0,2); upad.PaddingBottom=UDim.new(0,2); upad.Parent=scroll

local spamBtn=Instance.new("TextButton")
spamBtn.Size=UDim2.new(1,-6,0,spamBtnH)
spamBtn.Position=UDim2.new(0,3,1,-spamBtnH-3)
spamBtn.BackgroundColor3=C.spam
spamBtn.Font=Enum.Font.GothamBlack; spamBtn.Text="Spam Closest  (C)"
spamBtn.TextColor3=C.white; spamBtn.TextSize=tSp
spamBtn.ZIndex=4; spamBtn.Parent=LP
corner(spamBtn,6)
local sg=Instance.new("UIGradient")
sg.Color=ColorSequence.new({ColorSequenceKeypoint.new(0,C.spam),ColorSequenceKeypoint.new(1,C.spam2)})
sg.Rotation=90; sg.Parent=spamBtn

spamBtn.MouseEnter:Connect(function()
    TweenService:Create(spamBtn, TweenInfo.new(0.2), {BackgroundColor3=C.glow}):Play()
end)
spamBtn.MouseLeave:Connect(function()
    TweenService:Create(spamBtn, TweenInfo.new(0.2), {BackgroundColor3=C.spam}):Play()
end)

local function refreshCards()
    for _,c in pairs(cardMap) do pcall(function() c:Destroy() end) end
    table.clear(cardMap)
    for _,p in ipairs(Players:GetPlayers()) do
        if p==lp then continue end
        local card=Instance.new("TextButton")
        card.Name=p.Name
        card.Size=UDim2.new(1,-4,0,22)
        card.BackgroundColor3=C.card; card.Text=""; card.ZIndex=2; card.Parent=scroll
        corner(card,6)
        local cs=stroke(card,1.5,(selPlayer==p) and C.selBdr or C.border)
        local ava=Instance.new("ImageLabel")
        ava.Size=UDim2.new(0,14,0,14); ava.Position=UDim2.new(0,3,0.5,-7)
        ava.BackgroundColor3=C.card; ava.ZIndex=3
        local ok,img=pcall(Players.GetUserThumbnailAsync,Players,p.UserId,
            Enum.ThumbnailType.HeadShot,Enum.ThumbnailSize.Size48x48)
        ava.Image=ok and img or ""; ava.Parent=card; corner(ava,7)
        lbl(card,{Position=UDim2.new(0,20,0,2),Size=UDim2.new(1,-22,0,9),
            Font=Enum.Font.GothamBold,TextSize=tB,TextColor3=C.white,
            TextXAlignment=Enum.TextXAlignment.Left,Text=p.DisplayName,ZIndex=3})
        lbl(card,{Position=UDim2.new(0,20,0,11),Size=UDim2.new(1,-22,0,8),
            Font=Enum.Font.Gotham,TextSize=tS,TextColor3=C.grey,
            TextXAlignment=Enum.TextXAlignment.Left,Text="(@"..p.Name..")",ZIndex=3})
        card.MouseEnter:Connect(function()
            TweenService:Create(card, TweenInfo.new(0.15), {BackgroundColor3=Color3.fromRGB(35, 28, 20)}):Play()
        end)
        card.MouseLeave:Connect(function()
            TweenService:Create(card, TweenInfo.new(0.15), {BackgroundColor3=C.card}):Play()
        end)
        card.MouseButton1Click:Connect(function()
            selPlayer=p
            for _,c2 in pairs(cardMap) do
                local st=c2:FindFirstChildOfClass("UIStroke")
                if st then st.Color=C.border end
            end
            cs.Color=C.selBdr
        end)
        cardMap[p.UserId]=card
    end
end
Players.PlayerAdded:Connect(refreshCards)
Players.PlayerRemoving:Connect(function() task.wait(); refreshCards() end)
refreshCards()

local function spamClosest()
    if not core.remote then return end
    local hrp=lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    local best,bd=nil,math.huge
    for _,p in ipairs(Players:GetPlayers()) do
        if p~=lp and p.Character then
            local h=p.Character:FindFirstChild("HumanoidRootPart")
            if h then local d=(h.Position-hrp.Position).Magnitude
                if d<bd then bd=d; best=p end end
        end
    end
    if not best then return end
    local now=tick()
    for _,cmd in ipairs(CMDS) do
        if (cooldowns[cmd] or 0)<=now then
            pcall(function()
                core.remote:InvokeServer("f888ee6e-c86d-46e1-93d7-0639d6635d42",best,cmd)
            end)
            cooldowns[cmd]=now+DUR[cmd]
        end
    end
end
spamBtn.MouseButton1Click:Connect(spamClosest)

-- ══════════════════════════════════════════
-- RIGHT PANEL (COOLDOWNS)
-- ══════════════════════════════════════════
local RP = mkPanel(pos.rx, pos.ry, RW, RH)
local rpHdr, rpMinBtn = mkHeader(RP,"Cooldowns")
mkMinimize(RP, rpMinBtn)
draggable(rpHdr, RP,"rx","ry")

local rowY=HDR+4
for _,cmd in ipairs(CMDS) do
    local row=Instance.new("Frame")
    row.Size=UDim2.new(1,-6,0,ROW); row.Position=UDim2.new(0,3,0,rowY)
    row.BackgroundTransparency=1; row.Parent=RP
    lbl(row,{Position=UDim2.new(0,4,0,0),Size=UDim2.new(0.62,0,1,0),
        Font=Enum.Font.GothamBold,TextSize=tC,TextColor3=C.white,
        TextXAlignment=Enum.TextXAlignment.Left,Text=cmd})
    local sl=lbl(row,{Position=UDim2.new(0.62,0,0,0),Size=UDim2.new(0.38,-4,1,0),
        Font=Enum.Font.GothamBlack,TextSize=tSt,TextColor3=C.green,
        TextXAlignment=Enum.TextXAlignment.Right,Text="READY"})
    cdLabels[cmd]=sl
    rowY=rowY+ROW+2
end

RunService.RenderStepped:Connect(function()
    local now=tick()
    for _,cmd in ipairs(CMDS) do
        local l=cdLabels[cmd]
        if l and l.Parent then
            local cd=cooldowns[cmd] or 0
            if now>=cd then l.Text="READY"; l.TextColor3=C.green
            else l.Text=math.ceil(cd-now).."s"; l.TextColor3=C.red end
        end
    end
end)

UserInputService.InputBegan:Connect(function(i,gpe)
    if gpe then return end
    if i.KeyCode==Enum.KeyCode.C then spamClosest() end
end)
